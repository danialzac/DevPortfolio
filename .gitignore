# Editor / OS junk — ignore .claude EXCEPT the committed skills folder
.claude/*
!.claude/skills/
.DS_Store
.vscode

# Backup files
*.bak

# Separate project living in this folder — NOT part of the portfolio website.
# (If you want to showcase it, it should be its own repo + a project card.)
learning-app/
